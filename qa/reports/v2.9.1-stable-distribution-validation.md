# v2.9.1 Stable Distribution Validation

Date: 2026-05-17
Type: Lightweight post-release validation

## Homebrew Validation

Commands executed:

```bash
brew update
brew uninstall --force chuxorg/yanzi/yanzi
brew untap chuxorg/yanzi
brew tap chuxorg/yanzi /Users/developer/projects/chuxorg/homebrew-yanzi
brew install chuxorg/yanzi/yanzi
brew reinstall yanzi
"$(brew --prefix yanzi)/bin/yanzi" --version
```

Observed:

- Initial tap state resolved `2.9.1-rc1` (pre-normalization state).
- After tap normalization + formula checksum correction, Homebrew installed `2.9.1`.
- Version output: `yanzi v2.9.1`.

## Installer Validation

Commands executed:

```bash
./uninstall.sh
./install.sh
~/.local/bin/yanzi --version
./uninstall.sh
./install.sh --version=v2.9.1-rc1
```

Observed:

- Default install path resolved stable: `Yanzi v2.9.1 installed successfully.`
- Explicit RC request remained functional: `Yanzi v2.9.1-rc1 installed successfully.`
- Stable/default and RC/explicit semantics are now distinct and operational.

## Direct Binary Validation

Commands executed:

```bash
curl -fsSL https://github.com/chuxorg/yanzi/releases/download/v2.9.1/yanzi-darwin-arm64 -o /tmp/yanzi-v2.9.1.bin
chmod +x /tmp/yanzi-v2.9.1.bin
/tmp/yanzi-v2.9.1.bin --version
shasum -a 256 /tmp/yanzi-v2.9.1.bin ~/.local/bin/yanzi
```

Observed:

- Direct binary output: `yanzi v2.9.1`.
- Checksum parity confirmed between downloaded production binary and installer-installed binary:
  - `a34bd780037147952dfa64e028222c194c76d6a8bc70e2ba873700b5cc39c58f`

## Operational Observations

1. Homebrew lineage drift was primarily caused by stale tap state and stale formula checksums, not URL tag drift.
2. Deterministic installer default tag pinning removes release-page ordering ambiguity.
3. RC provenance remains intact and callable via explicit `--version` only.

## Conclusion
Stable distribution convergence is complete for v2.9.1 across Homebrew, installer default path, and direct release binary.
