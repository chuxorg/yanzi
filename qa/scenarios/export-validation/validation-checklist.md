# Validation Checklist

- [ ] Markdown export command exits `0` and output is readable.
- [ ] HTML export command exits `0` and output contains expected structure.
- [ ] JSON export command exits `0` and output parses cleanly.
- [ ] Export outputs represent consistent data across formats.
- [ ] Snapshot comparison result is recorded.
- [ ] Any baseline change is documented with release rationale.
